[project]
name = "communication-simulator"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "flask>=3.1.2",
    "openai>=2.43.0",
    "psycopg[binary,pool]>=3.3.4",
    "python-can>=4.6.1",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
