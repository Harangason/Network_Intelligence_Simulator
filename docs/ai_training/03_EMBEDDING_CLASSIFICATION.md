# Embedding Classification

The current implementation uses a local token-similarity adapter as a placeholder for embeddings. It has the same interface as a future vector backend and contributes weak evidence only.

Future vector retrieval must index approved concepts, approved signals and reviewed examples separately from unreviewed AI proposals.
