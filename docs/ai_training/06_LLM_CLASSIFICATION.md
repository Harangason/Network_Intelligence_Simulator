# LLM Classification

The LLM adapter is intentionally marked `PROPOSAL_ONLY_NOT_CALLED` in the first implementation.

When enabled later, LLM output must include structured evidence, uncertainty and alternatives. It must not write directly into the canonical model without review.
