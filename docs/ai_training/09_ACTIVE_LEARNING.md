# Active Learning

Active learning should prioritize cases with low confidence, ambiguous classification, model disagreement, unknown concepts, rare classes or high engineering impact.

The implementation currently exposes the required decision states so an active-learning queue can be built without changing classifier contracts.
