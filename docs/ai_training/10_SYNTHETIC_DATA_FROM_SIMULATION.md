# Synthetic Data From Simulation

Simulation may generate labeled training data only when each scenario has explicit labels.

Required label fields include scenario type, fault type, target, time window, expected effect, affected signals, affected messages and root cause.
