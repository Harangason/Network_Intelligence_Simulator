# Trace Training Data

Trace findings may become training examples after review. Raw trace data is not training data by itself.

Useful fields include trace window, signal values, messages, sequence, finding, root cause and confirmed diagnosis.
