# Graph Machine Learning

Graph ML is a future extension for link prediction, missing relation detection, node classification, critical node prediction, dependency prediction and routing recommendation.

It must use canonical relations and reviewed feedback, not temporary UI state.
