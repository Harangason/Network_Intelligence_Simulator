# LLM Adaptation

LLM improvement should prioritize context, ontology, retrieval and approved examples before fine-tuning or larger models.

Possible later steps are prompt optimization, few-shot retrieval, RAG improvement, instruction tuning, LoRA or QLoRA, domain adapters and preference training.
