# Device Classification Overview

HardwareNode classification separates physical existence from system-function generation.

Core fields: `device_class`, `device_typing`, `data_complexity`, `classification_status` and `capability_profile_ref`.
