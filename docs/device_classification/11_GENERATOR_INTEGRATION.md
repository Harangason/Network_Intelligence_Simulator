# Generator Integration

The generator consumes `hardware_information.generator_policy`.

The central decision is whether a device requires a dedicated function model or can remain a hardware participant with signals only.
