# Validation

The registry validates class, typing and data complexity combinations. Invalid manual combinations are normalized to the nearest valid default for the selected class before persistence.
