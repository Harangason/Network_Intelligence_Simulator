# Functional Interface View

The existing `Interface` view is the functional communication view.

It models:

- owning `Function`
- optional mapped `HardwareNode`
- logical interface type
- Messages and Signals exposed through that functional boundary

It is not used as the physical port/channel inventory.
