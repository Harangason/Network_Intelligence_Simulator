# Network Assignment

Every `HardwareNetworkInterface` may reference one `network_ref`.

Messages assigned to the same `network_ref` share the same bus capacity. Two physical interfaces on the same bus segment do not double available network capacity.
