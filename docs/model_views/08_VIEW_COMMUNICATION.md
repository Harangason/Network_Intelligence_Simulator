# View Communication

The implemented view links are:

- Hardware -> Hardware Interface through `hardware_node_id`
- Hardware Interface -> Message through `message.hardware_interface_id`
- Function -> Interface through `function_id`
- Interface -> Message through `interface_id`
- Message -> Signal through `message_id`

Workbench filters, sorting and details expose these links for navigation and inspection.
