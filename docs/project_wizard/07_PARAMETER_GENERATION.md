# Parameter Generation

Status: IMPLEMENTED

Verifikationsstand:
- Typische Parameter wie `sample_rate`, `horizontal_fov`, `resolution_x/y`, `bit_depth`, `overlap_deg` werden erzeugt.
- Status pro Parameter (`PROPOSED`, `KNOWN`, `ASSUMED`).

Offene Restpunkte:
- Vollständige, typisierte `KNOWN/PROPOSED/UNKNOWN/REQUIRED/OPTIONAL/DEFAULTED`-Governance ist noch auszubauen.
- Validierung gegen Regelwerke und Unitschema fehlt teilweise.

