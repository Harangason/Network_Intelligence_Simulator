# Signal Quality

Signal-Samples fuehren Quality-Metadaten:

- `VALID`
- `INVALID`
- `NOT_AVAILABLE`
- `ESTIMATED`

Aktuell wird `GENERIC_ESTIMATE` als `ESTIMATED` markiert. Nicht verfuegbare Werte, etwa durch Dropout, werden als `NOT_AVAILABLE` abbildbar.

