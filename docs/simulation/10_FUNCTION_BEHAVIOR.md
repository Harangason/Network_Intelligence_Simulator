# Function Behavior

`FunctionBehaviorEngine` bleibt die Bruecke zwischen bereits berechneten Input-Signalen und abhaengigen Output-Signalen.

Der aktuelle Stand nutzt Signalwerte im Event-Kontext fuer `STATE_DEPENDENT` und `FORMULA`. Die Runtime bleibt in Python; das Frontend visualisiert nur.

