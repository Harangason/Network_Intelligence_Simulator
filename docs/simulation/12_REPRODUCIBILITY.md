# Reproducibility

Reproduzierbarkeit basiert auf:

`Engineering Snapshot + Scenario + Seed = Reproducible Simulation`

Random Walk und Noise nutzen benannte, stabile Seed-Ableitungen. Es werden keine unkontrollierten globalen Zufallswerte fuer Signalwerte verwendet.

