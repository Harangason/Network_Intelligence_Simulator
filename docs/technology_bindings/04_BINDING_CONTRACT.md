# Binding contract

A binding maps a `FunctionalInterface` and ordered `TechnologyStack` to a `TechnologyBinding`. It carries hardware-interface and network references without turning either into protocol identity.

`BindingResolver` filters candidates by data complexity, bandwidth, determinism, safety, redundancy, domain, and hardware capabilities.
