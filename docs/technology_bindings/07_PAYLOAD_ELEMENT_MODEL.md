# PayloadElement model

Fields: `id`, `element_type`, `semantic_ref`, `data_type`, `size`, `unit`, `encoding`, `quality`, and `source_ref`.

The semantic datum is stable across technologies. Only its binding and technology-specific packing change.
