# Aerospace / avionics

ARINC 429 uses 32-bit `WORD` transport units. AFDX uses virtual-link packets over UDP/IP/Ethernet. MIL-STD-1553B uses command/data/status words. CAN Aerospace, SpaceWire, and time-triggered Ethernet are also registered with explicit status.
