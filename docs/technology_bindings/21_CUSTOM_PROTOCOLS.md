# Custom protocols

The registry contains Generic Serial/CAN/Ethernet and Custom UDP/TCP/Binary/Text/Protocol planning profiles. New proprietary implementations are added by registering components; the generic core and Wizard do not need protocol branches.
