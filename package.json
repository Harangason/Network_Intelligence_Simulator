{
  "name": "network-intelligence-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo-Wurzel für dieses Vercel-Multi-Service-Projekt (siehe vercel.json: services.frontend, services.backend). Diese Datei existiert nur, damit generische Dependency-Installationsschritte am Repo-Root nicht fehlschlagen - die eigentlichen Abhängigkeiten liegen in frontend/package.json (Next.js) und backend/pyproject.toml (Flask, via uv).",
  "scripts": {
    "dev": "start-networkis-local-ai.bat"
  }
}
