Die aktuelle Dokumentation befindet sich in README.md.

Primärer Standalone-Einstieg:

    uv run python communication_simulator.py --write-config-template simulation_config.json
    uv run python communication_simulator.py --config simulation_config.json

Die Anwendung unterstützt integrierte und frei definierbare Bus-Technologien.
